# Database
DATABASE_URL="postgresql://staylocal:staylocal@localhost:5432/staylocal?schema=public"

# NextAuth
AUTH_SECRET="reemplazar-con-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# Mail (MailHog local: smtp://localhost:1025)
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="StayLocal <no-reply@staylocal.local>"

# Pagos: 'fake' usa el adaptador local, 'stripe' usa Stripe sandbox
PAYMENT_PROVIDER="fake"
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
